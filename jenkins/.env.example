# Required during migration: set this to the existing Docker volume mounted at /var/jenkins_home.
JENKINS_HOME_VOLUME=

# Optional host path overrides.
GOBLIN_OBSERVATORY_REPO=/home/sarah/goblin-observatory
GOBLIN_DOCKER_CONFIG=/home/sarah/.docker
